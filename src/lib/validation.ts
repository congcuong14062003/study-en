import { z } from "zod";
export const passwordSchema = z.string().min(8, "Mật khẩu cần ít nhất 8 ký tự.").max(64, "Mật khẩu quá dài.").refine(v => new TextEncoder().encode(v).length <= 72, "Mật khẩu tối đa 72 byte UTF-8.").regex(/[a-zA-Z]/, "Mật khẩu cần có chữ.").regex(/[0-9]/, "Mật khẩu cần có số.");
export const registerSchema = z.object({ name: z.string().trim().min(2, "Nhập họ tên của bạn.").max(80), email: z.email("Email không hợp lệ.").toLowerCase().trim(), password: passwordSchema, confirmPassword: z.string() }).refine(v => v.password === v.confirmPassword, { message: "Mật khẩu xác nhận không khớp.", path: ["confirmPassword"] });
export const onboardingSchema = z.object({ goal: z.enum(["Giao tiếp", "Công việc", "Du lịch", "IELTS", "TOEIC", "TOEFL", "Học tập", "Khác"]), level: z.enum(["A1", "A2", "B1", "B2", "C1", "C2"]), dailyMinutes: z.number().refine(v => [10, 20, 30, 45, 60].includes(v)) });
export const noteSchema = z.object({ title: z.string().trim().min(1).max(120), content: z.string().max(12000), tags: z.array(z.string().trim().max(30)).max(10).default([]), resourceId: z.string().max(100).optional() });
export const courseSchema = z.object({ title: z.string().trim().min(3).max(120), level: z.enum(["A1", "A2", "B1", "B2", "C1", "C2"]), category: z.string().min(2).max(60), description: z.string().min(10).max(5000), duration: z.string().min(1).max(50), instructor: z.string().min(2).max(80), outcomes: z.array(z.string().max(200)).max(12).default([]), color: z.enum(["blue", "purple", "orange", "green"]).default("blue"), published: z.boolean().default(false), premium: z.boolean().default(false), thumbnail: z.string().regex(/^(https:\/\/[^\s]+|\/uploads\/[a-f0-9-]+\.(png|jpg|webp))?$/).nullable().optional() });
export const profileSchema = z.object({ name: z.string().trim().min(2).max(80), bio: z.string().max(500).optional(), dailyMinutes: z.number().int().min(10).max(60).optional(), reminderTime: z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).optional(), notificationsEnabled: z.boolean().optional(), publicLeaderboard: z.boolean().optional(), language: z.enum(["vi", "en"]).optional() });
